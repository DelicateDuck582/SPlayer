# ⚠️ 该文件会被提交进仓库（Vercel 构建依赖它注入 VITE_* 变量）。
# 因此**绝不要**在这里写入任何密钥 / token / 账号凭据；
# 需要密钥请改用 Vercel 项目环境变量（Project → Settings → Environment Variables）。
# 变量模板见 .env.example。

# WEB 端口
VITE_WEB_PORT=14558
# API 端口
VITE_SERVER_PORT=25884
# API 地址 - 结尾不要加 /
VITE_API_URL=https://music-api2.duckgame-play.top
